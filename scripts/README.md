# Reference build script

`build.py` is the audited script used for release 2.000. The path constants at the top point to the release build workspace. `SOURCES.md` records the exact upstream archive hashes used for the published binaries.
